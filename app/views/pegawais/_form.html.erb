<%= form_for(@pegawai) do |f| %>
  <% if @pegawai.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pegawai.errors.count, "error") %> prohibited this pegawai from being saved:</h2>

      <ul>
      <% @pegawai.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="field">
    <%= f.label :alamat %><br>
    <%= f.text_field :alamat %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
